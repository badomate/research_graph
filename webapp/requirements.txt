# webapp — server-rendered UI. Intentionally light: it only imports
# modules.store (SQLModel) and modules.metadata (stdlib), not the heavy pipeline.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
jinja2>=3.1,<4
python-multipart>=0.0.9
sqlmodel>=0.0.22,<0.1
webdavclient3==3.14.6
